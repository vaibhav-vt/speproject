---
- name: Pull docker image of calculator
  hosts: localhost
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Pull front-end image
      docker_image:
        name: vaibhavvt/proj-frontend:latest
        source: pull
    - name: Start docker service
      service:
        name: docker
        state: started
    - name: Running container
      shell: docker run -it -d --name FrontEnd vaibhavvt/proj-frontend